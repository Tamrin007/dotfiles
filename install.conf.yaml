- defaults:
    link:
        create: true
        relink: true
        force: true

- clean: ['~', '~/.config']

- link:
    ~/.config/nvim: config/nvim
    ~/.atom: atom
    ~/.slate: slate/slate
    ~/.zshrc: zsh/zshrc
    ~/.gitconfig: git/gitconfig
    ~/.gitignore: git/gitignore
