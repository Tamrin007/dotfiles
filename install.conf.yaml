- defaults:
    link:
        create: true
        relink: true
        force: true

- clean: ['~', '~/.config']

- link:
    ~/.config/nvim: config/nvim
    ~/.atom: atom
    ~/.slate: zsh/slate
    ~/.zshrc: zsh/zshrc
