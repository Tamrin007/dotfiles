- defaults:
    link:
        create: true
        relink: true
        force: true

- clean: ['~', '~/.config']

- link:
    ~/.config/nvim: config/nvim
    ~/.atom: atom
    ~/.slate: slate
    ~/.zsh: zsh
    ~/.zshrc: zshrc
